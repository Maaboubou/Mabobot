# 更新记录

## [3.4.0] - 2026-09-13

- 新增公开版翻译助手 2.0.1：每聊天双语／三语、语言顺序、提示词、可复用模板和网页试译；差异覆盖直接保存生效，保留微信表情与型号文本。
- 概览升级为运行状态、待处理、今日指标、24 小时活动、七日趋势、额度用量和事件时间线；增加增量聊天日志索引与独立实时脉搏。
- 统一控制台字号、圆角、阴影、明暗主题、表格列宽和格式化；改进用量列表整行操作、确认框可访问性和减少动态效果行为。
- 修复手机活动柱子重叠与裁切、回复段比例、时间线状态与筛选挤压；完善图表触屏和键盘读数。
- 修复概览用量汇总读取，待处理错误按最近复发时间退出；失败保留上次读数，不误报静默。
- 升级需要重启 Bot 与 Web 服务并刷新页面；保留本机配置，新配置表自动创建，备份格式与插件 Runtime API v2 不变。

完整公告见 [v3.4.0 发布说明](docs/releases/v3.4.0.md)。

## [3.3.0] - 2026-09-10

本版合并监听性能优化后重新发布，已下载用户请重新获取源码并重启。

- 严格确认尾部追加后减少旧消息头像方向重复探测，新消息仍完整识别，异常时回退完整读取。

- Codex 角色规则接入会话级指令，旧会话自动迁移；最终附件清单保留角色回复格式并校验本次文件来源。
- 新增启动器微信兼容性检查、版本化定位配置、报告对比与手动基线管理。
- 引用图片提前保存，读取失败时提示明确触发的用户；MP4 不再接收时预下载。
- 链接解析缩短桌面锁持有时间，原子处理文本剪贴板；长消息尾部恢复减少重复上翻与离屏解析。
- 网页摘要使用现有 Chrome 后台标签页，B 站登录态读取不切换交互标签页。
- AI 回复成功数按业务轮次统计，保留调用、Token 和费用记录；更新弹窗增加前台与遮挡校验。
- 更新依赖后完整重启启动器、Bot 与 Web 服务；保留本机配置。本版不变更备份格式或插件运行接口版本。

完整公告见 [v3.3.0](docs/releases/v3.3.0.md)。

## [3.2.2] - 2026-09-09

### 消息恢复与监听健康

- 消息游标恢复连续失败后进入冷却并定时重试，保留旧基线，避免停止恢复或跳过待交付消息。
- 修复定向滚轮步数、历史页面衔接、动画等待及失效控件矩形处理；恢复向下收集到确认底部，并保留页数和时间限制。
- 监听健康状态区分线程存活与消息交付正常，显示恢复、冷却、轮询错误和停滞状态；恢复交付后清除降级状态。
- 修复插件发现期间查询来源路径可能触发的注册表并发修改错误。

### 助手与网页体验

- 示例角色和判断器可编辑、删除，初始化标记避免重启后恢复已删除项；角色变更及时刷新聊天选项并保留未保存编辑。
- 插件设置支持选择推送接收对象，推送授权与消息处理授权独立；修复设置弹窗异步覆盖及保存状态问题。
- 消息消费遵循各监听器的传播策略，清理弃用的记忆、Detail 和 Hermes 模型路由。
- 生图使用默认官方 Codex Profile，由宿主交付生成图片，修正图片已发送后的超时提示。
- 修复监听窗口缩小后的恢复与多显示器枚举，为浏览器标签添加 Mabobot Logo，并完善 Tailscale 设置提示。

### 升级

- 更新源码后完整重启 Mabobot，使微信 Bot 与 Web 服务加载新代码；刷新网页获取静态资源。
- 保留现有 `.env`、`data/` 和本机配置。本版不变更备份格式或插件运行接口版本。

## [3.2.0] - 2026-09-08

本次重新发布整合原 v3.2.0、v3.2.1 及后续修复，统一使用 v3.2.0。此前下载过任一版本的用户需重新获取本次源码并完整重启。

### 历史消息与群昵称

- 实现 `GetHistoryMessage()` 历史上翻读取，支持数量、滚动间隔、速度、逐条回调、停止标记及返回最新位置；历史时间分隔值支持按日期截止。
- 历史读取和消息游标恢复共用定向滚动、页面变化等待与重叠校验，保留重复消息及控件 ID 复用下的独立消息，扩大有时限的旧游标查找范围。
- 页间等待释放全局 UI 锁；滚动与返回最新位置定向到目标窗口，减少鼠标干扰。
- 群昵称读取增加侧栏状态等待、同窗口恢复、焦点与点击位置校验，仅接受本人昵称设置行内的明确值；关闭侧栏时避免误关监听窗口。

### 启动与窗口体验

- 优化微信监听窗口的查找和创建，微信就绪后立即恢复已启用的监听，与 Web 和插件初始化并行进行；启动期间收到的消息等待 Web 就绪后转发。
- 修复微信所有窗口关闭到托盘后，重新唤起主窗口仍停在启动阶段的问题。
- 启动器首次关闭时可选择最小化到托盘或停止服务并退出，支持记住选择，并可在“设置 → 窗口行为”中随时修改。

### 稳定性

- 摘要权限查询只读取必要字段，避免无关数据库字段变更影响查询；未授权或查询失败时拒绝摘要处理。
- 微信链接卡片读取遇到浏览器菜单控件暂时失效时，在原超时范围内重新查找。
- 模型调用失败日志补充错误类型和状态码，便于区分额度、网络与服务端故障。

### 发送者识别与依赖整理

- 修复群聊发送者串名：取消跨消息身份缓存，每次重新探测头像，并补齐右键、中键、焦点读取顺序。
- 清理 Alembic 依赖；公开版同时移除 curl-cffi，整理 NumPy 依赖分组。

### 聊天档案与上下文

- 原始聊天档案替代生成式记忆，移除事件/人物/阶段提炼、向量引擎及旧管理接口；保留原文和成员别名迁移工具。
- 聊天档案支持分页查阅、检索、前后文、导出与成员昵称/别名维护；Codex 可按需读取当前聊天完整本地文件。
- 每个聊天复用独立 Codex 线程，首轮最多最近 50 条，后续按归档游标追加；支持原生 90% 压缩、异常恢复与累计轮数保留。
- 上下文窗口设置对官方登录和 API Key 均生效，运行中心按最新请求而非累计账单显示上下文。

### 用量与稳定性

- 新增请求级用量与计价账本，支持聊天/调用类型下钻，区分缓存读写、未知费用及 Codex API 等值估算。
- 修复 WebSocket 拦截同步关闭卡顿与健康详情阻塞 Web 事件循环。
- Weekly 跟随默认 Codex Profile 的实际模型；抖音下载复用时长检测元数据，并支持 iesdouyin 分享链接和浏览器 Cookie。

### 其他更新

- 引用视频同时提交四张抽帧和本地 SenseVoice 语音转写，明确转写不可靠；缺少模型时仅看截图，缺少 FFmpeg 或抽帧失败时降级文字回复并记录日志，消息处理中不自动下载依赖。
- 新增统一邮箱提醒配置：Web 控制台和启动器共用配置与测试入口，支持常用邮箱和自定义 SMTP，并按事件管理提醒。
- 新增游戏发售日历插件：核实当月主机与 PC 发售事实、生成日历长图，通过统一推送权限管理月度发布。
- Codex 辅助模型调用改为独立的单次文本任务，使用模型自身配置，保留结构化输出并准确记录实际模型。
- 微信成功发送的文件自动归档到引用索引，便于后续引用读取；索引失败不会将已成功的发送误报为失败。
- 调整助手来源链接的回复方式，并更新配置说明与插件推送开发规范。

## [3.1.0] - 2026-09-06

### 微信与多媒体

- 新增电子书下载插件：支持书名、作者、ISBN/DOI 解析、候选选择、格式与语言偏好以及下载前策略检查。
- 小红书图文合成长图时追加笔记正文和作者；修复聊天策略中插件开关的变更检测。

- 修复微信 Qt 窗口区域截图坐标错误：完整绘制窗口后再裁剪消息行，避免把窗口顶部误作缩略图，影响图片定位、点击和下载结果校验；新增逐像素回归测试。
- 加强图片下载诊断：关联接口请求与底层操作，记录 UI 排队、点击坐标、预览等待和 UIA 查找异常；预览超时时保留窗口状态快照，并恢复 mabowx 文件 DEBUG 日志。
- 新增引用视频问答：按需打开微信预览取得视频后，在本地无界面抽取 4 张等时段截图，仅将截图交给 Codex；模型会主动根据水印、字幕和账号等线索联网核查原始出处。

### 记忆系统

- 人物消息炼化和人物投影新增独立后台轮询：正常的 30 条/10 条门槛不变，低活跃人物分别在至少 8 条消息等待 14 天、或未投影观察等待 7 天后自动继续，不再依赖下一条群消息唤醒。
- 证据不足内容改为系统自动隔离的安全终态，不再呈现为必须人工清空的复核任务；人工入口只用于发现误判后的纠正、确认排除与撤销。
- 稳定 `sender_id` 已确认旧昵称归属时，自动合并残留的无 ID 重复身份并保留可撤销审计；无稳定证据的身份仍保持分离。
- 回复前通过本地规则过滤无关阶段摘要，普通问题不再仅因当前发送者存在画像就自动注入其资料；高敏感人物记忆恢复为默认不注入。
- 事件记忆仍保持 40 条核心消息加 12 条后文，阶段记忆仍保持每 40 张有效事件刷新，未降低两项阈值。


## [3.0.2] - 2026-09-04

### 新增

- `START.bat` 新增首次运行引导：缺少 Python 3.11/3.12（64 位）或 Microsoft WebView2 Runtime 时，经用户确认后从官方源下载安装并验证数字签名，无需新用户预先配置 Python。
- 首次安装窗口新增阶段百分比、Chromium 下载进度、实时命令输出、已用时间、最后活动时间、安装进程 PID、日志入口与安全取消操作；连续 5 分钟无新活动时会明确提示可能卡住。
- 微信掉线后会自动推进重新登录；需要扫码时截取当前登录二维码，并通过已配置邮箱发送内嵌二维码的安全提醒，恢复在线后继续发送恢复通知。

### 修复

- 关闭首次安装窗口时会确认并结束本次安装的完整进程树；再次运行 `START.bat` 若检测到旧引导残留，不再静默退出，而是允许清理旧进程并立即接管启动。
- 官方登录模式的 Codex Profile 现在可从编辑入口重新选择当前 ChatGPT 账号可用的模型，并同步模型支持的推理强度、上下文窗口和能力信息。
- 加强微信消息页重叠识别、锚点恢复和监听同步，降低翻页、窗口虚拟化及微信重连时的漏消息风险。
- 图片下载增加消息身份校验和可重试错误分类，本机内部通信强制绕过系统代理，并修正非零视口原点下的 Win32 坐标换算。

## [3.0.1] - 2026-09-02

### 修复

- Summary Plus 统一通过当前 Python 的 `-m yt_dlp` 调用视频下载器，不再依赖内嵌虚拟环境绝对路径的 `yt-dlp.exe`。
- 修复项目目录改名、移动或备份恢复后，Bilibili 等平台的下载任务立即退出且没有错误输出的问题。
- Web 端 yt-dlp 升级校验改为模块调用；下载失败时同时记录命令、stdout 和 stderr。

## [3.0.0] - 2026-09-02

### 重大变更

- 微信 UI 控制层完整迁移到项目内置的 `mabowx`，不再安装或加载旧自动化库。
- 产品、环境变量、文件工具、运行目录和启动入口统一使用 Mabobot 命名。
- 公开发行版只保留聊天记录、Magnet Check、菜单翻译、Summary Plus、Weekly 和中国银行汇率 6 个插件。

### 新增

- 新增独立桌面启动器，统一管理微信 Bot 与 Web 服务的启动、停止、重启、日志、环境检查和系统托盘。
- 新增 Codex Profile、Skill、聊天隔离、会话连续性、模型路由及文件处理能力；Codex CLI 作为执行框架，Profile 可使用 ChatGPT 账号或第三方 Responses 兼容模型。
- 微信附件建立持久索引，可在后续消息中精确绑定并交给 Codex 处理。

### 修复

- 显式声明 Windows 必需的 `pywin32`，修复全新安装后缺少 `win32api` 的问题。
- 安装器和启动引导会验证 mabowx 实际使用的全部 Win32 模块，并在依赖缺失时自动修复。
- Codex Profile、Skill、运行环境探测和升级命令统一按 UTF-8 读取 WSL 输出，避免 Windows 默认 GBK 导致页面无数据。
- 创建 Codex Profile 前会在实际 Linux/WSL 环境中解析 CLI 的绝对路径，避免新机把 `codex` 命令名误当作不存在的文件路径。

### 安全

- 群聊与普通私聊默认使用按聊天隔离的文件范围；只有显式授权的管理员私聊可以使用本机权限。
- 公开代码及发布增量执行凭据扫描，运行凭据、Cookie、数据库和聊天数据均不纳入版本控制。

[3.1.0]: docs/releases/v3.1.0.md
[3.0.2]: docs/releases/v3.0.2.md
[3.0.1]: docs/releases/v3.0.1.md
[3.0.0]: docs/releases/v3.0.0.md
